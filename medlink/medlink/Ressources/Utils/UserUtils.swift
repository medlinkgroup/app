//
//  UserUtils.swift
//  medlink
//
//  Created by Fabiana Montiel on 15/06/2020.
//  Copyright © 2020 Fabiana Montiel. All rights reserved.
//

import Foundation
/*
class UserUtils {
    
    //static var user: Doctor = UserUtils.getModeNotLogged()
    
    static func map(item: [String:Any]) -> Doctor? {
        guard
            let id = item["doctor_uid"] as? String,
            let first_name = item["name"] as? String,
            let family_name = item["familyName"] as? String,
            let phone_number = item["numberPhone"] as? String,
            let photo = item["photo"] as? String,
            let email = item["email"] as? String,
            let birthdate = item["birthDate"] as? String,
            let specialty = item["specialty"] as? String
            //let password = item["password"] as? String
        else {
            return nil
        }
        return Doctor(id: id, first_name: first_name, last_name: family_name, phone_number: phone_number, photo: photo, email: email, birth_date: birthdate, specialty: specialty/*, password: password*/)
        //return Doctor(id: id, email: email, password: password)
    }
    
    /*
    static func getModeNotLogged() -> Doctor {
        Doctor(id: "NA", email: ""/*, password: ""*/)
    }
    
    static func getModeAdministrator() -> Doctor {
        Doctor(id: "NA", email: "admin@live.fr", password: "test")
    }
    
    static func isLogged() -> Bool {
        return UserUtils.user.email != "" || UserUtils.isAdministrator()
    }
    
    static func isAdministrator() -> Bool {
        return UserUtils.user.email == UserUtils.getModeAdministrator().email
    }
     */
     
}*/
